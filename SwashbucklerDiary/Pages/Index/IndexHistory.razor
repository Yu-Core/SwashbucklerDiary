@namespace SwashbucklerDiary.Pages
@inherits ImportantComponentBase

<MyAppBar>
    @if (NormalCalendarVisible)
    {
        <MToolbarTitle Class="font-weight-bold">
            @(I18n.T("History.Calendar"))
        </MToolbarTitle>
    }
    else
    {
        <div class="float-calendar" style="cursor:pointer;" @onclick="()=>ShowFloatCalendar=true">
            <MTextField Value="PickedDate"
                        Class="rounded-lg text-h6 font-weight-bold"
                        Dense="true"
                        Readonly="true"
                        Solo="true"
                        Flat="true"
                        HideDetails="@("auto")"
                        PrependIcon="mdi-calendar">
            </MTextField>
        </div>
    }

    <MSpacer></MSpacer>
    <MButton Icon="true"
             OnClick="()=>ShowExportTime=true">
        <MIcon>
            mdi-export
        </MIcon>
    </MButton>
</MyAppBar>

<ScrollContainer @ref="ScrollContainer">
    <div @ref="NormalCalendar" class="normal-calendar">
        <MDatePicker @bind-Value="PickedDate"
                     Class="rounded-lg my-picker mb-5 pt-4"
                     TValue="DateOnly"
                     ShowCurrent="false"
                     FullWidth="true"
                     NoTitle="true"
                     Color="primary"
                     Events="EventsDates"
                     EventColor='"primary"'
                     Locale="@(I18n.Culture.Name)">
        </MDatePicker>
    </div>

    <DiaryCardList @bind-Tags="Tags"
                   Value="PickerDiaries"
                   OnUpdate="UpdateEventsDates">
    </DiaryCardList>
</ScrollContainer>

<MyDatePicker @bind-Value="ShowFloatCalendar"
              @bind-Date="PickedDate"
              Events="EventsDates"
              EventColor='"primary"'>
</MyDatePicker>

<MultiDialog @bind-Value="ShowExportTime"
             Title="@(I18n.T("History.ExportTime.Name"))"
             DynamicListItems="ExportTimeItems">
</MultiDialog>

<ExportDialog @bind-Value="ShowExport"
              Diaries="ExportDiaries">
</ExportDialog>
