@page "/sponsor"
@namespace SwashbucklerDiary.Rcl.Pages
@inherits ImportantComponentBase

<MyAppBar>
    <MButton Icon
             OnClick="NavigateToBack">
        <MIcon>
            arrow_back
        </MIcon>
    </MButton>

    <MyTabs @bind-Value="tab"
            Items="tabListItems">
    </MyTabs>

    <MSpacer></MSpacer>
</MyAppBar>

<SwiperTabItems @bind-Value="tab">
    <SwiperTabItem>
        <MContainer Class="my-container"
                    Fluid="true">
            <MCard Class="mb-5 user-select pt-2"
                   Elevation="0"
                   Rounded="@("lg")">
                <MCardTitle Class="text-h5 font-weight-bold py-sm-10" Style="justify-content:center;">
                    @(I18n.T("Support the development of Swashbuckler Diary"))
                </MCardTitle>
                <MCardText Style="text-indent: 2em;">
                    <MMarkdownIt Source="@whyDevelopDescription"
                                 Class="user-select">
                    </MMarkdownIt>
                </MCardText>
            </MCard>

            <MCard Class="mb-5"
                   Elevation="0"
                   Rounded="@("lg")">
                <MList Class="dynamic-list-item-collection">
                    @foreach (var item in sponsorTypeListItems)
                    {
                        <MListItem @key="item"
                                   OnClick="item.OnClick">
                            <MListItemIcon>
                                <MIcon Class="evident-icon">
                                    @(item.Icon)
                                </MIcon>
                            </MListItemIcon>
                            <MListItemContent>
                                @(I18n.T(item.Text))
                            </MListItemContent>
                            <MListItemAction>
                                <MIcon>chevron_right</MIcon>
                            </MListItemAction>
                        </MListItem>

                        <MDivider Class="mx-5"></MDivider>
                    }

                </MList>
            </MCard>
        </MContainer>
    </SwiperTabItem>
    <SwiperTabItem>
        <MContainer Class="my-container"
                    Fluid="true">
            <MSubheader Class="mb-2">@(I18n.T("Thanks to the following sponsor"))</MSubheader>
            <MCard Class="mb-5"
                   Elevation="0"
                   Rounded="@("lg")">
                <MList Class="dynamic-list-item-collection">
                    @foreach (var item in sponsors)
                    {
                        <MListItem @key="item">
                            <MListItemContent>
                                @item
                            </MListItemContent>
                        </MListItem>
                    }

                </MList>
            </MCard>
        </MContainer>
    </SwiperTabItem>
</SwiperTabItems>

<PreviewImageDialog @bind-Visible="showPreviewImage" Src="@src"></PreviewImageDialog>
