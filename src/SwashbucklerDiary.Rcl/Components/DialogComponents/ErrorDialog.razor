@namespace SwashbucklerDiary.Rcl.Components

<MDialog Value="true"
         ContentClass="rounded-lg"
         Persistent="true"
         Scrollable="true"
         MaxWidth="420">
    <MCard>
        <MCardTitle Class="font-weight-bold">
            <MIcon Class="error--text mr-2">
                cancel
            </MIcon>
            @(I18n.T("Error"))
        </MCardTitle>
        <MCardText Class="pb-0 scroll-show" Style="max-height:300px;">
            @Text
        </MCardText>
        <MCardActions>
            <MSpacer></MSpacer>
            <MButton Text="true"
                     Color="primary"
                     OnClick="CopyDetailedInformationAsync">
                @(I18n.T("Copy detailed information"))
            </MButton>
            <MButton Text="true"
                     Color="red"
                     OnClick="Quit">
                @(I18n.T("Quit"))
            </MButton>
        </MCardActions>
    </MCard>
</MDialog>

