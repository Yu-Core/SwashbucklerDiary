In order to provide other projects with a similar experience as Maui.Essentials, we ported the unpackaged Windows code from Maui.

- AppInfo (with deletions)
- FileSystem
- Preferences
- VersionTracking

Why choose unpackaged Windows code instead of others, because it does not contain platform specific code.

Need set Property ApplicationId
`<ApplicationId></ApplicationId>`